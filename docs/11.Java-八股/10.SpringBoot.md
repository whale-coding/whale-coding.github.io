---
title: SpringBoot
date: 2025-03-06 21:25:02
permalink: /pages/3c7dda
categories:
  - 八股-Java
  - 开发框架
---

### 1.说一说SpringMVC的执行流程？

:::note

SpringMVC 的执行流程大概是这样的：

首先，用户发起一个请求，会先被 **DispatcherServlet** 拦截到，它其实就是 SpringMVC 的前端控制器。

然后，DispatcherServlet 会根据请求的 URL，去找对应的 **HandlerMapping**，确定这个请求应该交给哪个 Controller 的方法来处理。

找到 Handler 之后，会通过 **HandlerAdapter** 来真正调用 **Controller** 的方法。Controller 方法执行完以后，会返回一个 **ModelAndView**，里面包含了数据模型和要跳转的视图名。

接下来，DispatcherServlet 会把这个 ModelAndView 交给 **ViewResolver**，去解析成具体的视图，比如 JSP、Thymeleaf 之类的。最后把数据渲染到视图里，再返回给用户。

总结一下，就是：SpringMVC 把请求统一交给前端控制器 DispatcherServlet，然后通过映射找到合适的 Controller 方法，执行完以后再通过视图解析器去渲染，最后把结果返回给前端。

:::

### 2.SpringBoot自动装配原理?

:::note

在主启动类上添加了@SpringBootApplication注解，这个注解组合了EnableAutoConfiguration注解。

EnableAutoConfiguration注解又组合了Import注解，导入了AutoConfigurationImportSelector类。

该类实现selectImports方法，这个方法经过层层调用，最终会读取`META-INF`目录下的后缀名为`.imports`的文件，当然了，在SpringBoot2.7以前的版本，读取的是spring.factories文件。

读取到全类名了之后，会解析注册条件，也就是@Conditional及其衍生注解，把满足注册条件的Bean对象自动注入到IOC容器中。

:::

### 3.如何自定义一个SpringBoot的Starter?

:::note

自定义 Spring Boot Starter 主要分为四步：创建 Maven/Gradle 项目并引入 `spring-boot-autoconfigure` 依赖；创建属性配置类（`XxxProperties`）；创建自动配置类（`AutoConfiguration`）；在 `META-INF/spring.factories` 或 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 文件中注册该配置类。

:::

### 4.SpringBoot启动流程？

:::note

SpringBoot的启动，其本质就是加载各种配置信息，然后初始化IOC容器并返回。

在其启动的过程中会做这么几个事情，

首先，当我们在启动类执行SpringApplication.run这行代码的时候，在它的方法内部其实会做两个事情：

1. 创建SpringApplication对象；
2. 执行run方法。

其次，在创建SpringApplication对象的时候，在它的构造方法内部主要做3个事情：

1. 确认web应用类型，一般情况下是Servet类型，这种类型的应用，将来会自动启动一个tomcat；
2. 从spring.factories配置文件中，加载默认的ApplicationContextInitializer和ApplicationListener；
3. 记录当前应用的主启动类，将来做包扫描使用。

最后，对象创建好了以后，再调用该对象的run方法，在run方法的内部主要做4个事情：

1. 准备Environment对象，它里面会封装一些当前应用运行环境的参数，比如环境变量等等；
2. 实例化容器，这里仅仅是创建ApplicationContext对象；
3. 容器创建好了以后，会为容器做一些准备工作，比如为容器设置Environment、BeanFactoryPostProcessor后置处理器，并且加载主类对应的Definition；
4. 刷新容器，就是我们常说的referesh，在这里会真正的创建Bean实例。

总结一下我刚说的，其实SpringBoot启动的时候核心就两步，创建SpringApplication对象以及run方法的调用，在run方法中会真正的实例化容器，并创建容器中需要的Bean实例，最终返回。

:::

### IOC容器的初始化流程？

IOC容器的初始化，核心工作是在AbstractApplicationContext.refresh方法中完成的.

在refresh方法中主要做了这么几件事：

1. 准备BeanFactory，在这一块需要给BeanFacory设置很多属性，比如类加载器、Environment等；
2. 执行BeanFactory后置处理器，这一阶段会扫描要放入到容器中的Bean信息，得到对应的BeanDefinition（注意，这里只扫描，不创建）；
3. 注册BeanPostProcesor，我们自定义的BeanPostProcessor就是在这一个阶段被加载的, 将来Bean对象实例化好后需要用到；
4. 启动tomcat；
5. 实例化容器中实例化非懒加载的单例Bean, 这里需要说的是，多例Bean和懒加载的Bean不会在这个阶段实例化，将来用到的时候再创建;
6. 当容器初始化完毕后，再做一些扫尾工作，比如清除缓存等 ；

简单总结一下，在IOC容器初始化的的过程中，首先得准备并执行BeanFactory后置处理器，其次得注册Bean后置处理器,并启动tomcat，最后需要借助于BeanFactory完成Bean的实例化。





### 5.SpringBoot核心特性有哪些？

Spring Boot的核心特性旨在简化Spring应用的初始化、开发和部署，核心在于**自动配置（Auto-configuration）**、**起步依赖（Starter Dependencies）**、嵌入式Web服务器（Embedded Servers）以及Actuator监控。这些特性实现了“约定大于配置”，让开发者能够快速构建独立、生产级别的应用。

:::note

- **[自动配置 (Auto-Configuration)](https://www.google.com/search?q=自动配置+(Auto-Configuration)&sca_esv=a6e1373de54531c1&biw=1542&bih=823&sxsrf=ANbL-n6BqSAaWmOD-WwHah_aZlJkHHns2A%3A1775474934993&ei=9pjTaYqiPI2p0-kP3tbp4Qs&ved=2ahUKEwjV6anUj9mTAxW11zQHHeOXHn0QgK4QegQIAxAB&uact=5&oq=SpringBoot核心特性有哪些？&gs_lp=Egxnd3Mtd2l6LXNlcnAiIlNwcmluZ0Jvb3TmoLjlv4PnibnmgKfmnInlk6rkupvvvJ8yCBAAGIAEGKIEMgUQABjvBTIFEAAY7wUyBRAAGO8FSPsCUABYAHAAeAGQAQCYAYMBoAGDAaoBAzAuMbgBA8gBAPgBAvgBAZgCAaAChwGYAwCSBwMwLjGgB7gBsgcDMC4xuAeHAcIHAzAuMcgHAoAIAQ&sclient=gws-wiz-serp&mstk=AUtExfABSRCTPvI7FhVjDcJvNEYanu_M1A4c4o0_GJOg8ElDJLXDlwCL-mIiMdWzrfqPOg4mxKxuTbDl2beooyMgoOb49-59tLheAJLZxUt5IgMd8BLKJhKi87hXqVTz5JmrzK9hPcMFiijB2r5OzOlx009Z6IS9UJki4vUk-XqcPm_X3VAnecD9z1visiCdYNZNiTJEX5D4UZ1jn34OwicDN8hBU2jDfM2ez-nmhqwT6Yoy1CEx4mmncWP1qgmkt2BiYuK9n7eWZpeyWKzORxkjT-CE&csui=3)**
  Spring Boot会自动根据类路径中的jar包依赖来配置Spring应用。例如，如果类路径中存在`HSQLDB`，且没有手动配置数据源，Spring Boot会自动配置一个内存数据库连接。
- 约定大于配置。

:::

### 6.SpringBoot常用注解？

:::note

SpringBoot常用注解包含核心启动类注解`@SpringBootApplication`，组件注册类`@Component`、`@Service`、`@Controller`、`@RestController`，依赖注入类`@Autowired`，以及配置类`@Configuration`与`@Bean`。这些注解极大简化了XML配置，实现了自动化配置与依赖注入。 

:::

### SpringBoot配置文件的加载顺序？

内部：结果优先级由高到底 .properties > .yml > .yaml

外部约定配置文件加载顺序：优先级 5>4>3>2>1

1. classpath根目录下的 
2. classpath根config/      
3. 项目根目录            
4. 项目根目录/config
5. 直接子目录/config



### 7.SpringBoot2和SpringBoot3的区别？

| 变更点              | Spring Boot 2.x     | Spring Boot 3.0                |
| ------------------- | ------------------- | ------------------------------ |
| **JDK 版本**        | JDK 8+              | **JDK 17+**                    |
| **Jakarta EE 迁移** | `javax.*`           | **`jakarta.\*`**               |
| **AOT 编译**        | 不支持              | **支持 GraalVM 原生镜像**      |
| **Observability**   | 仅 Actuator         | **Micrometer + OpenTelemetry** |
| **Spring MVC**      | 传统 `RestTemplate` | **新增 `@HttpExchange`**       |
| **Spring WebFlux**  | Reactor 3.4         | **Reactor 3.5**                |
| **Spring Security** | OAuth 2.0           | **OAuth 2.1 + PKCE**           |
| **数据层**          | Hibernate 5         | **Hibernate 6**                |
| **响应式增强**      | WebClient           | **WebFlux + Netty 强化**       |

:::note

- **Spring Boot 3.0 需要 JDK 17+**，不再支持低于 **Java 17** 的版本。
- 

:::









### 8.Spring和SpringBoot的区别？

:::note

:::

### 9.SpringBoot和SpringMVC的区别？

:::note

:::



### 10.Spring,Spring MVC,Spring Boot 之间什么关系?

:::note

:::
